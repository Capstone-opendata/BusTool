<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="capstone2015project.buscatchers">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="capstone2015project.buscatchers.StopToolSelectionActivity"
            android:label="@string/title_activity_stop_tool_selection"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="capstone2015project.buscatchers.ResultActivity"
            android:label="@string/title_activity_result"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="capstone2015project.buscatchers.NearbyActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_nearby"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="capstone2015project.buscatchers.FavoritesActivity"
            android:label="@string/title_activity_busstop_db"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="capstone2015project.buscatchers.StopsMapActivity"
            android:label="@string/title_activity_stops_map"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAcibZ8p9FRUwriaDIEiA1-bN86oRMbqTI" />

        <activity
            android:name="capstone2015project.buscatchers.FirstTimeSetupActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_first_time_setup"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
